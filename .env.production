# ==========================================
# PORTAL FRONTEND PRODUCTION CONFIGURATION
# ==========================================
# Environment configuration for independent portal frontend deployment
#
# Domain: kemetra.org, www.kemetra.org
# Service: Next.js Standalone Server
# ==========================================

# Application Environment
NODE_ENV=production

# Backend API URL
# The portal frontend will make API calls to this URL
NEXT_PUBLIC_API_BASE_URL=https://api.kemetra.org/api/v1

# ==========================================
# DEPLOYMENT NOTES:
# ==========================================
# 1. This frontend is a Next.js standalone server that calls the API
# 2. CORS must be configured in the API to allow kemetra.org
# 3. API must be accessible from the browser (not just server-to-server)
# 4. SSL certificates are managed by certbot in this deployment
# 5. NGINX serves as reverse proxy and terminates SSL
# 6. www.kemetra.org redirects to kemetra.org for SEO
# ==========================================

# ==========================================
# SECURITY CHECKLIST:
# ==========================================
# [ ] SSL certificates obtained for kemetra.org and www.kemetra.org
# [ ] API CORS configured to allow https://kemetra.org
# [ ] DNS A record points kemetra.org and www.kemetra.org to server IP
# [ ] Firewall allows ports 80/443
# [ ] Host NGINX routes kemetra.org to localhost:8446
# ==========================================
