# Production Environment Variables for Docker

# API Configuration - Connect to backend API
# Use domain name instead of IP for proper HTTPS and CORS
NEXT_PUBLIC_API_BASE_URL=https://api.kemetra.org/api/v1

# Environment
NODE_ENV=production
